## 1. ORM Model

- [x] 1.1 在 `src/models.py` 中增加 Document model（匹配 documents 表结构）

## 2. File Manager

- [x] 2.1 实现 `src/files/manager.py` — upload / list_files / delete_file / get_file_path

## 3. 验证

- [x] 3.1 创建 `scripts/test_file_manager.py` — 验证上传、格式校验、列表、删除（含物理文件清理）、get_file_path
