"""Benchmark query layer + scenario runners.

`queries.py` holds pure async SQL functions consumed by both
`scripts/bench/run_bench.py` (CLI basline) and `src/api/observability.py`
(Web UI tab in Phase 8.7). One query, two surfaces, same numbers.
"""
